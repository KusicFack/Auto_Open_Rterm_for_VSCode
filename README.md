# Description

Automatically starts an R terminal when opening .R or .qmd files in vscode, behaving like RSutdio.

# Installation

Put "local.auto-rterm-open" folder into path "~/.vscode/extensions", then execute command "Developer: Reload Window" in vscode(Ctrl/Cmd + Shift + P).  Installation is successful When this extension shows in extension:installed panel.